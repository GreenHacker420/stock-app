/**
 * Common regex patterns for backend validation.
 */

export const IS_DATE_REGEX = /^\d{4}-\d{2}-\d{2}$/;

// Add other common patterns here (e.g., CUID, SKU, etc.)
export const CUID_REGEX = /^c[a-z0-9]{24}$/;
